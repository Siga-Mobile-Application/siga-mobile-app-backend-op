name: "Qual Deploy"

on:
    push:
        branches:
            - 'qual'

permissions:
  id-token: write
  contents: read

jobs: 
    terraform:
        uses: ./.github/workflows/pipeline.yml
        with: 
            environment: qual